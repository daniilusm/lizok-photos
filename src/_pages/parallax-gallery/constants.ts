export const GALLERY_IMAGES = Array.from(
  { length: 10 },
  (_, index) => `/parallax-gallery/${index + 1}.webp`,
);

export const SCROLL_EASE = 0.07;
export const PARALLAX_INTENSITY = 0.4;
export const COLOR_MASK_RADIUS = 0.8;
export const CURSOR_DISTORT_INTENSITY = 0.01;
export const POINTER_SMOOTHING = 0.14;
export const MEDIA_BORDER_RADIUS = 30;
